import { Checkbox } from '@material-ui/core'
import React from 'react'

const FormCheckBox: React.FC = () => {
  return <Checkbox color="primary" inputProps={{ 'aria-label': 'secondary checkbox' }} />
}

export default FormCheckBox
